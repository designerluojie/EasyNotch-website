# 依赖
node_modules/

# 构建产物（GitHub Actions 会在 CI 里自行构建，不入库）
dist/
*.tsbuildinfo

# 测试产物
test-results/
playwright-report/
blob-report/

# 本地环境变量覆盖（.env.production 是有意入库的部署配置，故不忽略）
.env.local
.env.*.local

# 系统与编辑器
.DS_Store
.idea/
*.swp
